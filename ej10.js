var d1, d2; 
var num1, num2; 
function js(){
for(var j=0; j<=36000; j++){
    for(var i=1; i<=6; i++){
       return Math.random(num1 == d1 && num2 == d2);
       
    }
    var resultado=d1 + d2; 
}
document.write(resultado); 
}